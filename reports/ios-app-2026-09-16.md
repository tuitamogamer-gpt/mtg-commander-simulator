# iOS app preparation — 16 September 2026

Prepared on Windows using the Build iOS Apps SwiftUI guidance. The app targets iPhone/iPad on iOS 17+, with Capacitor 8.5.2 and an exact Swift Package Manager dependency.

## Verified locally

- `npm.cmd run check`: passed.
- Focused tests: 15 distinct checks passed (5 baseline, 6 account client, 4 iOS packaging/export checks). The full engine suite was not run for this shell change.
- `npm.cmd run ios:sync`: passed; 6,860 runtime files, approximately 447.5 MiB.
- New JavaScript files pass Node syntax checking.
- Xcode project parsed successfully; all 6 Swift source files are registered, with iOS 17 deployment settings in every configuration.
- Property lists, privacy manifest, storyboards and shared scheme parsed as XML. This is structural validation, not Swift compilation.
- `npm.cmd audit`: zero reported vulnerabilities. A scoped `xcode → uuid 11.1.1` override avoids the CLI's vulnerable old UUID dependency; project parsing and UUID generation were checked.
- In-app browser at 390 × 844: deck library, search, First Flight spotlight, one-AI pod, review, mulligan keep and playing Plains in main phase succeeded. No captured warning/error console entries during the game check.
- At 1024 × 768: battlefield and hand inspected, document width equals viewport width (no page-level horizontal overflow).
- Mac ZIP: 6,894 files, approximately 404.2 MiB; ZIP CRC and selected source-file parity verified. It contains the prepared Xcode project and bundled runtime without node_modules, account data or workstation credentials.

Archive SHA-256: `469284657af313b676e347f96c64949db6e61055a2bd8c4db3bc364a6cad2611`.

## Requires the user's Mac

Xcode compilation, simulator execution, native SwiftUI appearance, WKWebView custom-scheme loading, native dialogs/share sheet, signing and installation have **not** been executed on Windows. The browser check runs the bundled web content in Chromium, not in Apple's WebKit. See `docs/ios.md` for the native device checklist. The included macOS CI workflow has not been dispatched.

No commit, push, production deployment, TestFlight upload or App Store submission was performed.
