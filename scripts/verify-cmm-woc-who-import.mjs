import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';import {execFileSync} from 'node:child_process';import crypto from 'node:crypto';
import {loadEngine} from '../tests/helpers/load-engine.mjs';import {buildIntake,sourceDir} from './import-cmm-woc-who-precons.mjs';
const baseline='f674dd7',sha=x=>crypto.createHash('sha256').update(x).digest('hex'),git=(...a)=>execFileSync('git',a,{encoding:'utf8',maxBuffer:30_000_000}),old={},now={};
vm.runInNewContext(git('show',baseline+':src/data.js'),old);vm.runInNewContext(fs.readFileSync('src/data.js','utf8'),now);
for(const[n,c]of Object.entries(old.MTG.RAW_DATA.cards))assert.equal(JSON.stringify(now.MTG.RAW_DATA.cards[n]),JSON.stringify(c),n);
for(const d of old.MTG.RAW_DATA.decks)assert.equal(JSON.stringify(now.MTG.RAW_DATA.decks.find(x=>x.name===d.name)),JSON.stringify(d),d.name);
vm.runInNewContext(git('show',baseline+':src/card-images.js'),old);vm.runInNewContext(fs.readFileSync('src/card-images.js','utf8'),now);
for(const key of ['CARD_IMAGE_PATHS','CARD_ART_PATHS'])for(const[n,p]of Object.entries(old.MTG[key]))assert.equal(now.MTG[key][n],p,n);
assert.equal(git('diff','--name-only','--diff-filter=DMRTUXB',baseline,'--','assets').trim(),'','previously tracked artwork or video changed');
const dataSha=sha(fs.readFileSync('src/data.js'));execFileSync(process.execPath,['scripts/import-cmm-woc-who-precons.mjs','--write'],{stdio:'pipe',maxBuffer:5_000_000});assert.equal(sha(fs.readFileSync('src/data.js')),dataSha,'idempotent raw data');
const M=loadEngine(),intake=buildIntake(M),oracle=JSON.parse(fs.readFileSync(sourceDir+'/oracle.json')).cards,images=JSON.parse(fs.readFileSync(sourceDir+'/images.json'));
const normalize=s=>String(s||'').replaceAll('’',"'").replaceAll('—','-').replace(/\([^()]*(?:\([^()]*\)[^()]*)*\)/g,' ').replace(/\s+/g,' ').trim();
const retainedOracleDifferences=oracle.flatMap(r=>{const name=M.resolveDeckCardName(r.requestedName),d=M.DEFS[name];return normalize(d.oracle)===normalize(r.raw.oracle)?[]:[{name,existingOracle:d.oracle,pinnedOracle:r.raw.oracle,preserved:true}];});
for(const r of images.added)assert.equal(sha(fs.readFileSync(r.relative)),r.sha256,r.name);
const result={baseline,passed:true,originalPhysicalCards:500,uniqueNames:intake.names.length,sourceLists:intake.decks.map(d=>({name:d.name,total:d.cards.reduce((n,c)=>n+c.n,0),independentSources:[d.source.official,d.source.mtgjson],exportSha256:d.source.sha256})),existingRawDefinitionsPreserved:Object.keys(old.MTG.RAW_DATA.cards).length,existingRawDeckRecordsPreserved:old.MTG.RAW_DATA.decks.length,nativeRawDefinitions:Object.keys(now.MTG.RAW_DATA.cards).length,existingImageMappingsPreserved:Object.keys(old.MTG.CARD_IMAGE_PATHS).length,existingCommanderCropsPreserved:Object.keys(old.MTG.CARD_ART_PATHS).length,existingTrackedAssetsUnchanged:true,addedWebPFiles:images.addedCount,allNewImageDigestsMatch:true,idempotentDataSha256:dataSha,retainedOracleDifferences,current:{decks:Object.keys(M.DECKS).length,definitions:Object.keys(M.DEFS).length,eligible:Object.values(M.CARD_CATALOG).filter(c=>c.deckImportEligible).length}};
fs.writeFileSync(sourceDir+'/source-validation.json',JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({...result,retainedOracleDifferences:retainedOracleDifferences.map(r=>r.name),sourceLists:result.sourceLists.length},null,2));
